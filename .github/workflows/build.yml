name: Build VHCheat

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build with clang
        run: |
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          CC=$(xcrun --sdk iphoneos --find clang)
          echo "SDK: $SDK"
          echo "CC: $CC"
          
          cp VHCheat7.xm VHCheat7.m
          
          $CC -arch arm64 \
            -isysroot "$SDK" \
            -miphoneos-version-min=14.0 \
            -fobjc-arc \
            -framework Foundation \
            -framework UIKit \
            -framework CoreGraphics \
            -framework QuartzCore \
            -dynamiclib \
            -install_name @executable_path/VHCheat7.dylib \
            -o VHCheat7.dylib \
            VHCheat7.m
          
          brew install ldid
          ldid -S VHCheat7.dylib
          
          file VHCheat7.dylib
          ls -la VHCheat7.dylib
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: VHCheat7.dylib
          path: VHCheat7.dylib
