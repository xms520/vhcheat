name: Build VHCheat

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build with clang
        run: |
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          CC=$(xcrun --sdk iphoneos --find clang)
          echo "SDK: $SDK"
          echo "CC: $CC"
          
          cp VHCheat11.xm VHCheat11.m
          
          $CC -arch arm64 \
            -isysroot "$SDK" \
            -miphoneos-version-min=14.0 \
            -fobjc-arc \
            -framework Foundation \
            -framework UIKit \
            -framework CoreGraphics \
            -framework QuartzCore \
            -dynamiclib \
            -install_name @executable_path/VHCheat11.dylib \
            -o VHCheat11.dylib \
            VHCheat11.m
          
          brew install ldid
          ldid -S VHCheat11.dylib
          
          file VHCheat11.dylib
          ls -la VHCheat11.dylib
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: VHCheat11.dylib
          path: VHCheat11.dylib
