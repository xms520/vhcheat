name: Build VHCheat

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build with clang
        run: |
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          CC=$(xcrun --sdk iphoneos --find clang)
          echo "SDK: $SDK"
          echo "CC: $CC"
          
          cp VHCheat4.xm VHCheat4.m
          
          $CC -arch arm64 \
            -isysroot "$SDK" \
            -miphoneos-version-min=14.0 \
            -fobjc-arc \
            -framework Foundation \
            -framework UIKit \
            -framework CoreGraphics \
            -framework QuartzCore \
            -dynamiclib \
            -install_name @executable_path/VHCheat4.dylib \
            -o VHCheat4.dylib \
            VHCheat4.m
          
          brew install ldid
          ldid -S VHCheat4.dylib
          
          file VHCheat4.dylib
          ls -la VHCheat4.dylib
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: VHCheat4.dylib
          path: VHCheat4.dylib
