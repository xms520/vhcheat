name: Build VHCheat

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build with clang
        run: |
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          CC=$(xcrun --sdk iphoneos --find clang)
          echo "SDK: $SDK"
          echo "CC: $CC"
          
          cp VHCheat10.xm VHCheat10.m
          
          $CC -arch arm64 \
            -isysroot "$SDK" \
            -miphoneos-version-min=14.0 \
            -fobjc-arc \
            -framework Foundation \
            -framework UIKit \
            -framework CoreGraphics \
            -framework QuartzCore \
            -dynamiclib \
            -install_name @executable_path/VHCheat10.dylib \
            -o VHCheat10.dylib \
            VHCheat10.m
          
          brew install ldid
          ldid -S VHCheat10.dylib
          
          file VHCheat10.dylib
          ls -la VHCheat10.dylib
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: VHCheat10.dylib
          path: VHCheat10.dylib
