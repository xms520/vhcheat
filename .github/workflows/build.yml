name: Build VHCheat

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build with clang
        run: |
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          CC=$(xcrun --sdk iphoneos --find clang)
          echo "SDK: $SDK"
          echo "CC: $CC"
          
          cp VHCheat9.xm VHCheat9.m
          
          $CC -arch arm64 \
            -isysroot "$SDK" \
            -miphoneos-version-min=14.0 \
            -fobjc-arc \
            -framework Foundation \
            -framework UIKit \
            -framework CoreGraphics \
            -framework QuartzCore \
            -dynamiclib \
            -install_name @executable_path/VHCheat9.dylib \
            -o VHCheat9.dylib \
            VHCheat9.m
          
          brew install ldid
          ldid -S VHCheat9.dylib
          
          file VHCheat9.dylib
          ls -la VHCheat9.dylib
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: VHCheat9.dylib
          path: VHCheat9.dylib
