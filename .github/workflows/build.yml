name: Build VHCheat

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build with clang
        run: |
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          CC=$(xcrun --sdk iphoneos --find clang)
          echo "SDK: $SDK"
          echo "CC: $CC"
          
          cp VHCheat5.xm VHCheat5.m
          
          $CC -arch arm64 \
            -isysroot "$SDK" \
            -miphoneos-version-min=14.0 \
            -fobjc-arc \
            -framework Foundation \
            -framework UIKit \
            -framework CoreGraphics \
            -framework QuartzCore \
            -dynamiclib \
            -install_name @executable_path/VHCheat5.dylib \
            -o VHCheat5.dylib \
            VHCheat5.m
          
          brew install ldid
          ldid -S VHCheat5.dylib
          
          file VHCheat5.dylib
          ls -la VHCheat5.dylib
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: VHCheat5.dylib
          path: VHCheat5.dylib
