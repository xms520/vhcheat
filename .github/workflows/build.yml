name: Build VHCheat

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build with clang
        run: |
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          CC=$(xcrun --sdk iphoneos --find clang)
          echo "SDK: $SDK"
          echo "CC: $CC"
          
          cp VHCheat6.xm VHCheat6.m
          
          $CC -arch arm64 \
            -isysroot "$SDK" \
            -miphoneos-version-min=14.0 \
            -fobjc-arc \
            -framework Foundation \
            -framework UIKit \
            -framework CoreGraphics \
            -framework QuartzCore \
            -dynamiclib \
            -install_name @executable_path/VHCheat6.dylib \
            -o VHCheat6.dylib \
            VHCheat6.m
          
          brew install ldid
          ldid -S VHCheat6.dylib
          
          file VHCheat6.dylib
          ls -la VHCheat6.dylib
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: VHCheat6.dylib
          path: VHCheat6.dylib
